<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlumniHub"
        tools:targetApi="31">
        <activity
            android:name=".activities.CreateEventPostFormScreen"
            android:exported="false" />
        <activity
            android:name=".activities.TestScreen"
            android:exported="false" />
        <activity
            android:name=".activities.AdditionalDetailsFormScreen"
            android:exported="false" />
        <activity
            android:name=".activities.SignUpScreen"
            android:exported="false" />
        <activity
            android:name=".activities.LoginScreen"
            android:exported="false" /> <!-- SplashScreen ta launcher screen korlam -->
        <activity
            android:name=".activities.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />
    </application>

</manifest>